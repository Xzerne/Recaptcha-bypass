{
  "version": 2,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.js"
    }
  ],
  "builds": [
    {
      "src": "/index.js",
      "use": "@vercel/node"
    }
  ],
"env": {
    "PUPPETEER_SKIP_CHROMIUM_DOWNLOAD": "true" // Skip Chromium download if handled manually
  }
}
